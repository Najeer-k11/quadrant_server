name: quadrant_server_example
description: A production-ready reference application for the quadrant_server package.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  quadrant_server:
    path: ..

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
